# Tracking Limitations

A detected load means the tracking image URL was requested. It is not proof that a person read the email.

Image proxying and privacy protection can preload or cache images. Security scanners can trigger loads. Recipients may block images entirely. Multi-recipient messages have aggregate attribution. The UI therefore uses terms such as **Open detected**, **Tracking image loaded**, and **Likely privacy-protected load**.
